numpy>=1.26
scipy>=1.11
pandas>=2.1
scikit-learn>=1.3
matplotlib>=3.8
seaborn>=0.13
librosa>=0.10
soundfile>=0.12
noisereduce>=3.0.0
webrtcvad>=2.0.10
praat-parselmouth>=0.4.3
pyyaml>=6.0.2
omegaconf>=2.3.0
joblib>=1.3
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.6
shap>=0.45

# GPU acceleration dependencies (optional, for CUDA-enabled systems)
# Install CuPy for CUDA 11.x (GTX 1660 Super compatible):
# pip install cupy-cuda11x
# Or for CUDA 12.x:
# pip install cupy-cuda12x
# 
# Install RAPIDS cuML for GPU-accelerated ML:
# conda install -c rapidsai -c conda-forge -c nvidia cuml cudf cudatoolkit=11.8
# Or via pip (if available for your CUDA version):
# pip install cuml-cu11  # for CUDA 11.x
# 
# Note: GPU packages are large and CUDA-version specific.
# The code will automatically fall back to CPU if GPU libraries are not available.

# Optional heavy dependencies (uncomment as needed)
# torch>=2.4
# torchvision>=0.19
# torchaudio>=2.4
# tensorflow>=2.16

